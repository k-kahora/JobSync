* Set AWS_PROFILE=admin then you can run packer build
# TODO
- [ ] @replace_me in the database endopint url in install_latest.sh script
- [ ] build the entire environment with terraform
- [ ] pass the endopint into user data using sed with the packer build up ami id
